#!/bin/sh
# shell script to prepend i3status with more stuff

i3status | while :
do
     read line
     playerctl --player=$PLAYER metadata --format '{{title}} - {{artist}}' > /tmp/now_playing
     echo $line || exit 1
done

